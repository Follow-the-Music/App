<p id="notice"><%= notice %></p>
<style>
  body{
    background-image: url('<%= asset_path("jam.jpg")%>');
    background-repeat: repeat;
  }

  .jam{
    color: black;
    background-color: #F8F8FF;
    text-align:center;
  }
  h1{
    font-family: "Rockwell Extra Bold", "Rockwell Bold", monospace;
    font-size: 40px;
    font-style: normal;
    font-variant: normal;
    font-weight: 500;
    line-height: 26.4px;
    text-align:center;
  }
  /*.button2 {
    background-color:  #F8F9FF;
    border-radius: 10%;
    border-color:black;
    font-size: 27px;
    font-family: "Rockwell Extra Bold", "Rockwell Bold", monospace;

  }*/
</style>
<div class="jam">
  <br>
<h1><strong><%=@jam_session.name%></strong></h1>
<br>
<p>
  <strong>Host:</strong>
  <%= @name[0]%>
</p>

<p>
  <strong>Number of players:</strong>
  <%=  @jam_players_count  %> / <%=@jam_session.max_players%>
</p>

<p>
  <strong>Number of listeners:</strong>
  <%= @jam_listeners_count  %> / <%=@jam_session.max_listeners%>
</p>
<table align="center",style="text-align:center;">
  <thead>
    <tr>
      <th style="text-align:center;">Participants</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to 'Show', user %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<p>
  <strong>Description:</strong>
  <%= @jam_session.description %>
</p>
<br>
<br>
<%= link_to "Join", guest_session_association_path(id:session.id,player:TRUE), class:'btn btn-danger' %>
<br><br>
<hr>
<p>
  <h1>Chat</h1>
  <%= render 'chat_messages/form', chat_message: ChatMessage.new, jam_session: @jam_session %>
  <br>
  <% @chat_messages.reverse.each do |message| %>
    <%= render 'chat_messages/message', message: message %>
  <% end %>
</p>

<br><br>
<%= link_to 'Edit', edit_jam_session_path(@jam_session) %> |
<%= link_to 'Map', jam_path %> |
<%= link_to 'Your Sessions', guest_session_associations_path %>
<br><br>
</div>

</body>
